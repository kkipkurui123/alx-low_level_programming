#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int _isupper(int c);

int main(void)
{
	int c;

	if ((c = 'C'))
	{
	return (1);
	}

	else (c = 'c');
	{
	return (0);
	}
}
